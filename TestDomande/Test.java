public class Test {


    public static void main(String[] arg) {

        int[] vet = new int[]{10, 20, 30, 40, 50};
        for (int i = 0; i < vet.length; i++) {
            System.out.println(vet[i]/i);
        }


        System.out.println(240 | 15);

        // il codice genera una eccezione java.lang.ArithmeticException
        // Vero
    }

    /*
    public static void main(String[] arg) {
        String analizzata = " Oggi devo prendere l'ombrello !";
        System.out.println(analizzata);
        analizzata = analizzata.replace("!", "?");
        System.out.println(analizzata);

        // il codice trasforma una affermazione in domanda
        // Vero
    }
    */
    /*
     Se una classe contiene un metodo astratto allora deve essere dichiarata astratta (abstract)
     // Vero

     */
/*
        Gli array consentono di memorizzare collezioni di dati omogenei (dello stesso tipo)
        Vero

        esistono elaborazioni che possono essere realizzate con un ciclo for, ma non con un ciclo while
        // Falso

        Non è mai possibile effettuare una operazione di cast, come la seguente, che trasformi un valore double in int
        es.
        double a = 10.45987;
        int b = (int) a;
        Falso


    public static void main(String[] arg) {
        double c = 10342.4534543;
        int d = (int) c;

        System.out.println(d);

        // il codice trasforma una affermazione in domanda
        // Vero
    }
     */


}


